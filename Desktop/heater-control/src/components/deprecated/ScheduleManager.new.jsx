import React from 'react';
import { ScheduleManager as ScheduleManagerComponent } from './schedule';

const ScheduleManager = (props) => {
  return <ScheduleManagerComponent {...props} />;
};

export default ScheduleManager;